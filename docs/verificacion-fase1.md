# Lista de Verificación - Fase 1: Maquetación

## Páginas del sitio
- [x] Las 3 páginas enlazadas: `index.html`, `proyecto.html` y `conduce.html`.

## Estructura Semántica HTML5
- [x] Uso de `<header>`.
- [x] Uso de `<nav>`.
- [x] Uso de `<main>`.
- [x] Uso de `<section>`.
- [x] Uso de `<footer>`.

## Elementos requeridos
- [x] Existencia de listas (ej. en la navegación y en index/proyecto).
- [x] Existencia de tabla (ej. en `conduce.html`).
- [x] Existencia de imagen (ej. logo en `index.html`).
- [x] Formulario visual (en `conduce.html`).

## Uso de CSS
- [x] CSS externo (archivo `estilos.css`).
- [x] CSS interno (ej. bloque `<style>` en `index.html`).
- [x] CSS en línea (ej. atributo `style` en el enlace y en la imagen de `index.html`).
- [x] Selectores de etiqueta (ej. `body`, `h2`, `p`).
- [x] Selectores de clase (ej. `.bienvenida`, `.logo-empresa`).
- [x] Selectores de ID (ej. `#acceso-formulario`, `#numeroConduce`).

## Diseño y Navegación
- [x] Misma identidad visual y navegación en las páginas (mismo header, nav, estilos generales y footer).